export interface IContactInterface {
    fullname: string;
    email: string;
    message: string;
}

export interface IContactErrorInterface {
    fullname: boolean;
    email: boolean;
    message: boolean;
}
