import styles from '@/styles/Services.module.css';

const Hero = () => {
    return (
        <section className={styles.services}>
            <h1>Our Services</h1>
        </section>
    );
};

export default Hero;
