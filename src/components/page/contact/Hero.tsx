import styles from '@/styles/Contact.module.css';

const Hero = () => {
    return (
        <section className={styles.contact}>
            <h1>Contact Us</h1>
        </section>
    );
};

export default Hero;
